#!/bin/bash

outputPath="output"
list=("scalar" "${outputPath}/s.redberry" "axial" "${outputPath}/a.redberry" "tensor" "${outputPath}/t.redberry")

function process(){
	echo "./bbcc cc -f `echo "$@"` > `echo "${2}Progress"`"
	# ./bbcc cc -f `echo "$@"` > `echo "${2}Progress"`
}

export -f process
export list
echo ${list[@]} | tr ' ' '\n' | parallel --gnu -N 2 process "$@" {1} {2}